<!doctype html>
<html lang="zh-CN">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>{{.Title}}</title>
	<link rel="icon" href="/favicon.png?v=20260518-transparent" type="image/png">
	<link rel="shortcut icon" href="/favicon.ico?v=20260518-transparent">
	<link rel="stylesheet" href="/static/admin.css?v=20260521-admin-user-chip">
</head>
<body class="admin-page">
	<header class="admin-topbar">
		<a class="brand" href="/admin"><img src="/static/brand/koimoe-logo-header.png" alt="{{.Site.Name}}"></a>
		<nav>
			<a href="/admin">Posts</a>
			<a href="/admin/comments">Comments</a>
			<a href="/admin/links">Links</a>
			<a href="/admin/moments">Moments</a>
			<a href="/admin/media">Media</a>
			<a href="/admin/taxonomy">Taxonomy</a>
			<a href="/admin/settings">Settings</a>
			<a href="/" target="_blank">View Site</a>
			<form method="post" action="/admin/logout"><button type="submit">Logout</button></form>
		</nav>
	</header>
	<section class="admin-hero">
		<div>
			<p>Friends</p>
			<h1>Keep the small doors warm.</h1>
		</div>
		<div class="admin-user-chip" aria-label="{{.Site.Author}}"><img src="{{.Site.Avatar}}" alt="{{.Site.Author}}"><span>{{.Site.Author}}</span></div>
	</section>
	<main class="admin-shell">
		<section class="admin-panel">
			<div class="panel-title">
				<h1>Links</h1>
				<a class="button primary" href="/admin/links/new">New Link</a>
			</div>
			{{if .Message}}<div class="admin-alert success">Saved.</div>{{end}}
			<table class="admin-table">
				<thead><tr><th>Name</th><th>Category</th><th>Status</th><th>Order</th><th></th></tr></thead>
				<tbody>
					{{range .FriendLinks}}
					<tr>
						<td><a href="/admin/links/{{.ID}}/edit">{{.Name}}</a><span>{{.URL}}</span></td>
						<td>{{.Category.Name}}</td>
						<td><span class="status {{if .Visible}}published{{else}}hidden{{end}}">{{if .Visible}}visible{{else}}hidden{{end}}</span></td>
						<td>{{.SortOrder}}</td>
						<td class="table-actions">
							<a href="{{.URL}}" target="_blank" rel="noopener noreferrer">Open</a>
							<form method="post" action="/admin/links/{{.ID}}/delete"><button class="danger" type="submit">Delete</button></form>
						</td>
					</tr>
					{{else}}
					<tr><td colspan="5">No links yet.</td></tr>
					{{end}}
				</tbody>
			</table>
		</section>
	</main>
	<script src="/static/admin.js?v=20260521-nav-init"></script>
</body>
</html>
